import React from 'react';
import ReactDOM from 'react-dom/client';
import '../index.css';

const navButton = ReactDOM.createRoot(document.getElementById('navButton'));
navButton.render(
  <React.StrictMode>
    <div class="experience-box">
        testing
    </div>
  </React.StrictMode>
);
